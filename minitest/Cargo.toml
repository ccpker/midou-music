[package]
name = "rodio-test"
version = "0.1.0"
edition = "2021"

[dependencies]
rodio = { version = "0.20", default-features = false, features = ["mp3"] }
reqwest = { version = "0.12", features = ["blocking"] }
