# 酷狗音乐接入维护文档

> 项目：midou-music（Tauri v2 + Vue 3 + Rust reqwest 直连）
> 更新：2026-08-21
> 参考库（逆向来源）：`D:\workspaces\search\KuGouMusicApi`（= MoeKoeMusic 的 api 子模块，node 版）

---

## 一、核心结论（先记住这 5 条，能少踩 80% 坑）

1. **酷狗有两套独立的 API 体系，token 不互通**：
   - **web 平台**（扫码登录，plat=4）：token 只能用于搜索，**永远拿不到 vip_token**
   - **android 概念版**（plat=1，appid=3116）：token 带 vip_token/vip_type，能播 VIP、能签到
2. **播放走 `/v5/url`，不是 `/v2/get_res_privilege/lite`**。后者现在只返回权限信息（无 URL），已废弃。`/v5/url` 用「扫码 token + dfid」就能拿到 VIP 完整 URL，**无需 vip_token**。
3. **签名公式统一**：`MD5(salt + 排序后的key=value串 + body + salt)`，但**签名参数集必须是完整参数集**（dfid/mid/uuid/appid/clientver/clienttime + 业务参数）。少任何一个字段签名就错（20006 err signature）。
4. **salt 有三套，别混**：
   | 场景 | salt |
   |------|------|
   | android 概念版签名（搜索/播放/歌单） | `LnT6xpN3khm36zse0QzvmgTZ3waWdRSA` |
   | web 扫码登录签名 | `NVPh5oo715z5DIWAeQlhMDsWXXQV4hwt` |
   | signKey（播放 key） | `185672dd44712f60bb1736df5a377e82` |
5. **VIP 状态藏在 `busi_vip` 数组里**，不在主 `is_vip` 字段。当前账号是概念版「畅听 VIP」（tvip），主字段 `vip_type=0` 是正常的。

---

## 二、常量速查

| 常量 | 值 | 位置 |
|------|-----|------|
| 概念版 appid | `3116` | `platform/kugou.rs` |
| 概念版 clientver | `11440`（播放接口覆盖为 11430） | 同上 |
| 概念版签名盐 | `LnT6xpN3khm36zse0QzvmgTZ3waWdRSA` | 同上 |
| signKey 盐 | `185672dd44712f60bb1736df5a377e82` | 同上 `sign_key()` |
| 网关 | `https://gateway.kugou.com` | 同上 |
| 默认 dfid | `2-a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6`（硬编码假值） | `kugou.rs` / `kugou_login.rs` |
| web 登录盐 | `NVPh5oo715z5DIWAeQlhMDsWXXQV4hwt` | `commands/kugou_login.rs` |
| 扫码 srcappid | `2919` | 同上 |
| web clientver | `11309` | 同上 |

---

## 三、登录链路（扫码）

**文件**：`src-tauri/src/commands/kugou_login.rs`（自建 Rust 直连，零外部依赖）

流程：`kugou_qr_key`（取码）→ 前端弹二维码 → `kugou_qr_check`（轮询）→ `kugou_save_qr_token`（存 DB）。

**三处 appid 必须这样设**（曾因用错导致 token 平台错）：

| 环节 | 接口 | appid 取值 |
|------|------|-----------|
| 取二维码 | `/v2/qrcode` | `1001` |
| qrcode_txt 内嵌 | `...index.html?appid=3116&` | `3116` |
| 轮询 | `/v2/get_userinfo_qrcode` | `3116` |

- 取码参数额外带：`type=1, plat=4, srcappid=2919, qrcode_txt=...`
- 轮询 `status==4` = 登录成功，返回 `token` + `userid` + `nickname`
- **注意**：轮询返回**不含 vip_token/vip_type/t1**，所以扫码登录态下的 `KugouAuth.vip_token` 永远是空（这是设计使然，不是 bug）

**登录态存储**（`db.rs` 的 `kugou_auth` 表，INSERT OR REPLACE id=1）：

```
dfid / token / userid / vip_token / vip_type / token_expires / updated_at
```

- `token_expires` 当前写死 `now + 86400`（1 天），实际酷狗 token 约 24h 有效
- `vip_token` / `vip_type` 扫码后写空 / 0

---

## 四、搜索链路

**文件**：`platform/kugou.rs` 的 `search()`

- 接口：`GET /v3/search/song`，`x-router: complexsearch.kugou.com`
- 关键参数：`platform=AndroidFilter`、`page`（1-based，内部 page+1 转换）、`pagesize=20`
- 签名：完整参数集 + android 概念版盐
- 登录态注入 `token/userid`；匿名态不注入（避免空值污染签名）
- 返回字段映射：`FileHash`→song_id 的 hash、`AlbumID`→album_id、`FileName`→歌名、`SingerName`→歌手、`AlbumName`→专辑、`Duration`→秒
- **song_id 约定**：`kugou:{FileHash}|{AlbumID}`（无 album 则 `kugou:{FileHash}`）

---

## 五、播放链路（最关键）

**文件**：`platform/kugou.rs` 的 `play_url()`

- 接口：`GET /v5/url`，`x-router: trackercdn.kugou.com`
- **为什么不是 `/v2/get_res_privilege/lite`**：那个接口只返回权限（`relate_goods`，无 URL），现在不返回播放地址了
- 关键参数（对齐 moekoe `song_url.js`）：
  - `clientver=11430`（覆盖默认 11440）、`page_id=967177915`、`pid=411`、`ppage_id=356753938,823673182,967485191`、`cmd=26`、`pidversion=3001`、`ssa_flag=is_fromtrack`、`cdnBackup=1`、`IsFreePart=0`
  - `album_id` 从 song_id 拆出
- **signKey**（encryptKey 机制）：`key = MD5(hash_lower + "185672dd44712f60bb1736df5a377e82" + appid + mid + userid)`
- 登录态注入 `token/userid`（**扫码 token 即可拿完整 URL**）
- UA：`Android15-1070-11083-46-0-DiscoveryDRADProtocol-wifi`（与搜索/歌单 UA 不同）
- 返回：顶层 `url[]` 数组第一个元素是完整播放地址（`backupUrl[]` 是备用）

**验证过的结论**：扫码 token（web 平台）调 `/v5/url` 能拿到 VIP 歌完整版（如《青花瓷》3.83MB 非 60s 试听），前提是账号有 tvip 权益（见第八节）。

---

## 六、歌单链路

**文件**：`platform/kugou.rs` + `commands/kugou_playlist.rs`

### 歌单列表
- 接口：`POST /v7/get_all_list`，`x-router: cloudlist.service.kugou.com`
- body：`{userid, token, total_ver:979, type:2, page:1, pagesize:100}`
- **签名坑**：必须用完整参数集（dfid/mid/uuid/appid/clientver/clienttime + plat/userid/token）。**早期只拿 plat/userid/token 三个参数签名 → 20006**，这是历史 bug。
- 返回 `data.info[]`，关键字段：`listid`、`global_collection_id`（如 `collection_3_1514557990_2_0`）、`listname`、`m_count`

### 歌单内歌曲
- 接口：`GET /pubsongs/v2/get_other_list_file_nofilt`（无 x-router）
- 参数：`global_collection_id` + `begin_idx`（0-based）+ `pagesize` + `area_code=1` + `plat=1` + `type=1` + `mode=1` + `personal_switch=1` + `extend_fields`
- 返回 `data.songs[]`：`hash`、`album_id`、`name`（含"歌手 - 歌名"前缀，需拆分）、`singerinfo[].name`、`timelen`（**毫秒**，需 /1000）、`albuminfo.name`、`cover`
- song_id 同搜索约定

---

## 七、歌词链路

**文件**：`platform/lyric.rs` + `commands/lyric.rs`

三级回退：**酷狗官方 → LRCLIB 精确 → LRCLIB 模糊**

### 酷狗官方（source=kugou 且有 hash 时）
1. 搜索：`GET http://lyrics.kugou.com/search?ver=1&man=yes&client=pc&keyword={urlencode(name)}&duration={ms}&hash={urlencode(file_hash)}`
   - 返回 `candidates[0].id` + `accesskey`
2. 下载：`GET http://lyrics.kugou.com/download?ver=1&client=pc&id={id}&accesskey={ak}&fmt=lrc&charset=utf8`
   - **返回 JSON 包裹的 `content` 字段是 base64 编码的 LRC**，必须 base64 解码（`decode_base64_lrc()`），否则歌词解析 0 行

### LRCLIB
- 精确：`GET https://lrclib.net/api/get?track_name=&artist_name=`
- 模糊：`GET https://lrclib.net/api/search?q=name+singer`
- 提取：优先 `syncedLyrics`，其次 `plainLyrics`

---

## 八、VIP 机制（当前账号实测 2026-08-21）

查询接口：`GET https://kugouvip.kugou.com/v1/get_union_vip`，params `{busi_type:concept, opt_product_types:dvip,qvip, product_type:svip}`

当前账号（userid=1514557990）实测：
- 主字段 `is_vip=0`、`vip_type=0`
- **但 `busi_vip[]` 数组里有 `product_type=tvip`（概念版畅听 VIP），`is_vip=1`，到期 2026-09-15**

**结论**：手机端签到送的「概念版畅听 VIP」藏在 `busi_vip` 数组，主字段体现不出来。能播 VIP 歌靠的就是这个 tvip 权益。

### 签到领 VIP 接口（参考库 `module/youth_*.js`，均需 android 登录态）
| 接口 | 用途 |
|------|------|
| `POST /youth/v1/ad/play_report` | 看广告领 N 小时 VIP |
| `POST /youth/v1/recharge/receive_vip_listen_song` | 每日领 1 天（params: source_id=90139, receive_day） |
| `POST /youth/v1/listen_song/upgrade_vip_reward` | 升级概念版（params: kugouid, ad_type=1） |
| `GET /v1/get_union_vip` | 查 VIP 状态 |

**注意**：签到接口写死 `encryptType: 'android'`，需要 android token（扫码的 web token 会报 51002）。

---

## 九、已知坑汇总

| 坑 | 现象 | 解法 |
|----|------|------|
| 签名参数集不全 | 20006 err signature | 用完整参数集签名 |
| 走旧播放接口 | 拿不到 URL | 改用 `/v5/url` |
| 歌词 content 是 base64 | 歌词 0 行 | base64 解码 |
| 扫码 token 查 VIP | 主字段 is_vip=0 | 查 `busi_vip` 数组 |
| 扫码 token 调签到接口 | 51002 | 签到需 android token |
| `timelen` 单位 | 前端时长放大 1000 倍 | /1000 转秒 |
| 歌单歌曲 name 带前缀 | 显示"歌手 - 歌名" | `split_song_name()` 拆分 |

---

## 十、代码结构速查

```
src-tauri/src/
├── platform/
│   ├── kugou.rs          # 搜索/播放/歌单（android 概念版签名）
│   ├── lyric.rs          # 歌词三级回退
│   ├── kuwo.rs           # 酷我（零 Cookie）
│   └── bili.rs           # B站（DASH 零鉴权）
├── commands/
│   ├── kugou_login.rs    # 扫码登录（web 签名）
│   ├── kugou_playlist.rs # 歌单命令
│   ├── lyric.rs          # get_lyric 命令
│   ├── play.rs           # song_id 前缀路由
│   ├── search.rs         # 搜索路由
│   └── window.rs         # 播放条窗口
├── types.rs              # Song/KugouAuth/AppState
└── db.rs                 # kugou_auth 表存储

src/
├── composables/
│   ├── useKugouLogin.ts     # 扫码登录前端流程
│   ├── useKugouPlaylist.ts  # 歌单前端流程
│   ├── useLyric.ts          # 歌词解析
│   └── usePlayer.ts         # 播放流程
└── components/
    ├── Sidebar.vue          # 侧边栏（含歌单三级菜单）
    ├── PlayerBar.vue        # 播放条（独立窗口）
    └── KugouLogin.vue       # 扫码登录弹窗
```

### song_id 路由规则（`play.rs`）
| 前缀 | 音源 |
|------|------|
| 纯数字 | 酷我 MUSIC_RID（默认） |
| `au{id}` / `bv{id}` | B站 |
| `kugou:{hash}` | 酷狗 |

---

## 十一、参考库位置（逆向时可查）

`D:\workspaces\search\KuGouMusicApi\`（node 版，可 `node app.js --platform=lite --port=6521` 起本地服务）

关键参考文件：
- `util/config.json` — 常量（appid/clientver/salt/wx 密钥）
- `util/request.js` — 签名注入逻辑（完整参数集 + signatureAndroidParams）
- `util/helper.js` — 各签名函数（signKey/signatureAndroidParams/signParams 等）
- `util/crypto.js` — RSA/AES 加密（密码登录用）
- `module/login_qr_*.js` — 扫码登录参考
- `module/song_url.js` — `/v5/url` 播放参考
- `module/youth_*.js` — 签到领 VIP 参考
- `module/lyric.js`（如有）— 歌词参考

---

## 十二、未来待办 / 未完成

1. **VIP 完整播放**：当前靠 tvip 权益（9-15 到期），到期后需走「签到领 VIP」（youth 系列，需 android token）
2. **android token 获取**：扫码只能拿 web token，android token 需密码登录（撞 20028 滑块）或短信登录（撞 WAF）
3. **密码登录**：`login_by_pwd` 已逆向（`test_login_pwd4.js` 可参考），但被腾讯滑块风控拦截，未突破
4. **dfid 真实注册**：`register_dev` 已验证能拿真实 dfid，但当前代码仍用硬编码假 dfid
5. 侧边栏歌单三级菜单已接，但「取消收藏/红心」等操作未做
