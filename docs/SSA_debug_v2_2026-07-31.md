# SSA 验证调试 2026-07-31

## 本轮修改（逐行对照 crypto.js）

| 问题 | 修复前 | 修复后 |
|------|--------|--------|
| `get_verify_info` sid/edt | 传了生成好的值 | **空字符串**（对齐 `get_verify_info.js`） |
| `rsa_raw_encrypt` (pk) | 曾误改成 PKCS1v1.5 | **raw RSA modPow**（对齐 `cryptoRSAEncrypt`） |
| 缺少详细日志 | 没有 | 加了 url/body/raw resp 日志 |
| `verify_user_info` 响应读取 | `.json()` | `.text()` + 手动 parse + 日志 |

## 日志位置

- `D:\workspaces\search\projects\midou-music-v2\src-tauri\debug.log`
- 或 `%APPDATA%\com.midou.music\logs\`

## 状态

0 errors ✅ | 编译通过 | 待用户实测 + 拿日志定位
