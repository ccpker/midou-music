# SSA 滑块验证前端对接 — 流水线完成

**时间**: 2026-07-30 16:19–16:31

## 改了什么

### Rust 后端
- `types.rs` → `PlayUrlResult` 加 `ssa_code: Option<String>` 字段（`skip_serializing_if`）
- `kugou.rs` → 新增 `KugouPlayResult` struct；`play_url()` 返回类型改为 `KugouPlayResult`
- `kugou.rs` → `v5_play_url()` 返回类型也改为 `KugouPlayResult`，捕获 `ssa-code` response header
- `kugou.rs` → kmr/v6/v5 三条链路全部包装为 `KugouPlayResult { url, ssa_code }`
- `kugou.rs` → v5 20028 时：若 `ssa-code` header 非空，返回 `KugouPlayResult { url: "", ssa_code: Some(...) }` 而非报错
- `commands/play.rs` → 适配新返回类型，酷狗分支传入 `ssa_code`

### 前端
- `RiskVerifyModal.tsx` → 完整实现：
  - 调 `ssa_prepare` 获取 `txappid/v_type/sessionid/edt/sid/pk/params`
  - 动态加载腾讯 TCaptcha.js
  - 弹出滑块 → 拿到 ticket/randstr → 拼 `KGCodeTX|{ticket,randstr,txappid}`
  - 调 `ssa_verify` 提交验证
  - 成功/失败/取消三态
- `App.tsx` → `handlePlay` 检测 `ssa_code` 字段，有则弹出 `RiskVerifyModal`
- `App.tsx` → `handleSsaSuccess` 验证成功后自动重试播放

## 编译状态
- `cargo check`: 0 errors（7 pre-existing warnings）
- `tsc --noEmit`: clean

## 待验证
- 真实触发 20028 → 收到 ssa_code → 前端弹滑块 → 全链路通
- TCaptcha.js CDN 在国内 Tauri WebView 里的加载兼容性
