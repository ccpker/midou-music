# 登录状态迁移 — tauri-plugin-store

## 2026-07-30 08:33~08:51

### 目标
token 持久化从 SQLite 搬到 `tauri-plugin-store`，登录板块自闭环，不再依赖 AuthContext。

### 改动

| 文件 | 操作 | 变化 |
|------|------|------|
| `Cargo.toml` | +1 | `tauri-plugin-store = "2"` |
| `package.json` | +1 | `@tauri-apps/plugin-store` |
| `src-tauri/src/db.rs` | -110行 | 删 kugou_auth 表 + 3 函数 |
| `src-tauri/src/lib.rs` | 改1行 | load_kugou_auth → default_fallback；命令列表更新 |
| `src-tauri/src/commands/kugou_login.rs` | 重写 | 删 save/status，加 kugou_set_auth |
| `src/components/KugouLogin.tsx` | 重写 | LazyStore + 模块缓存，自闭环 |
| `src/App.tsx` | -5行 | 删 AuthProvider |
| `src/components/Sidebar.tsx` | 重写 | 删 auth 依赖 |
| `src/hooks/AuthContext.tsx` | 作废 | 不再需要 |

### 新架构
```
store.json ←→ KugouLogin.tsx ←→ 酷狗API
                 │
          invoke("kugou_set_auth")
                 ↓
          Rust 内存缓存(play_url用)
```

### 修复
- 切页面登录态丢失 → 加 `cachedAuth` 模块级变量（会话缓存）
- 编译通过、tsc 零错误
